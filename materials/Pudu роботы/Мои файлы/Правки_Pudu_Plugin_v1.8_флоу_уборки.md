# Правки прототипа Плагин Pudu — Флоу уборки (замечания №2)

> Дата: 2026-02-18  
> Прототип: `front-pudu-plugin`  
> Версия: 1.8 (unreleased)

---

## Суть изменений

Исправлены два флоу уборки посуды: перед выбором столов теперь **обязательно показывается экран «Выбор робота»** (бывший «Статус роботов»), чтобы пользователь мог видеть, какой робот свободен, и сделать осознанный выбор, прежде чем перейти к выбору столов.

---

## Замечание 1: Флоу «Дополнения → Pudu: Команды → Уборка (Столы)»

### Было (старый флоу)
```
Дополнения (кнопка)
  → окно «Плагины»
    → Pudu: Команды
      → окно «Команды роботам»
        → Уборка (Столы)
          → окно «Уборка посуды: Выберите столы для уборки» (cleanup_multi_select)
```

### Стало (новый флоу)
```
Дополнения (кнопка)
  → окно «Плагины»
    → Pudu: Команды
      → окно «Команды роботам»
        → Уборка (Столы)
          → окно «Выбор робота» (статусы роботов с кнопкой «Выбрать столы для уборки →»)
            → окно «Уборка посуды: Выберите столы для уборки» (cleanup_multi_select)
```

**Ключевое изменение заголовка:** В этом флоу заголовок окна статусов роботов меняется с «Статус роботов» на **«Выбор робота»**.

---

## Замечание 2: Флоу из каталога «Уборка посуды (мультивыбор)»

### Было (старый флоу)
```
Каталог → ячейка «Уборка посуды (мультивыбор)»
  → окно «Уборка посуды: Выберите столы для уборки» (cleanup_multi_select)
```

### Стало (новый флоу)
```
Каталог → ячейка «Уборка посуды (мультивыбор)»
  → окно «Выбор робота» (статусы роботов с кнопкой «Выбрать столы для уборки →»)
    → окно «Уборка посуды: Выберите столы для уборки» (cleanup_multi_select)
```

**Ключевое изменение заголовка:** В этом флоу заголовок окна статусов роботов меняется с «Статус роботов» на **«Выбор робота»**.

---

## Технические изменения (для спецификации)

### Окно «Статус роботов» / «Выбор робота» (М18)

Окно получило поддержку режимов отображения через параметры:

| Параметр | Тип | Описание |
|---|---|---|
| `title` | `string` | Заголовок окна. Default: «Статус роботов» |
| `subtitle` | `string` | Подзаголовок. Default: «Текущее состояние всех роботов ресторана» |
| `proceedLabel` | `string \| null` | Если задан — показывается акцентная кнопка действия перед кнопкой «Закрыть» |

**В режиме «Выбор робота»** (флоу уборки):
- Заголовок: **«Выбор робота»**
- Подзаголовок: **«Выберите свободного робота для выполнения уборки»**
- Кнопка действия: **«Выбрать столы для уборки →»** (акцентный цвет, цвет `#b8c959`)
- После нажатия на кнопку → открывается окно «Уборка посуды: Выберите столы для уборки»

**В штатном режиме «Статус роботов»** (просмотр из меню):
- Заголовок: **«Статус роботов»**
- Подзаголовок: **«Текущее состояние всех роботов ресторана»**
- Только кнопка «Закрыть»

### UX-поведение окна

Содержимое окна идентично в обоих режимах:
- Таблица роботов: Имя / ID / Статус (🟢 Свободен / 🟠 Занят / ⚫ Оффлайн) / Текущая задача
- Кнопка «Обновить» (имитирует загрузку 1.5 сек)
- Метка «Последнее обновление: HH:mm:ss»

---

## Обновлённые флоу (полная спецификация)

### Флоу уборки с главного экрана (П-У-1) — ОБНОВЛЁННЫЙ

1. Кассир нажимает **«Дополнения»** → открывается меню «Плагины»
2. Кассир нажимает **«Pudu: Команды»** → открывается окно «Команды роботам»
3. Кассир нажимает **«Уборка (Столы)»** → открывается окно **«Выбор робота»**
4. Кассир видит список роботов со статусами (свободен/занят/оффлайн)
5. Кассир смотрит, какой робот свободен, и нажимает **«Выбрать столы для уборки →»**
6. Открывается окно **«Уборка посуды: Выберите столы для уборки»**
7. Кассир выбирает столы галочками → нажимает **«Отправить робота»**
8. Команда отправляется роботу (fire-and-forget), появляется toast «Отправлено»

### Флоу уборки из каталога (для тестирования) — ОБНОВЛЁННЫЙ

1. Из каталога состояний кассир нажимает ячейку **«Уборка посуды (мультивыбор)»**
2. Открывается окно **«Выбор робота»** (те же шаги 4–8 что выше)

---

## Что НЕ изменилось

- Флоу уборки **из контекста заказа** (`cleanup_confirm`) — без изменений, по-прежнему сразу показывает окно уборки одного стола.
- Само окно «Уборка посуды: Выберите столы» — не изменилось.
- Логика отправки задачи роботу — не изменилась.
- Каталог состояний не изменился (ячейка `ctx-main-cleanup-multi` переводит сначала на выбор робота, но через логику в pos-screen).
