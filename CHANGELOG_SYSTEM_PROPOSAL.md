# Предложение: Система Changelog в прототипах

> **Цель**: каждый прототип показывает текущую версию, дату обновления и имеет удобный просмотр истории изменений — понятный для заказчика и команды разработки.

---

## Текущее состояние

Сейчас changelogs ведутся в **markdown-файлах** (`changelogs/*.md`). Они:
- Не видны пользователю прототипа (доступны только в репозитории)
- Не имеют версионирования (только даты)
- Записи не группируются по страницам
- Нет кликабельных ссылок на страницы

---

## Что нужно реализовать

| Требование | Описание |
|---|---|
| **Версия** | Номер текущей версии прототипа, видимый в UI |
| **Дата обновления** | Дата последнего изменения (без времени) |
| **Кнопка «Changelog»** | Открывает полную историю изменений |
| **Группировка по страницам** | Изменения на одной странице объединены вместе |
| **Кликабельные страницы** | Название страницы — ссылка, ведущая к этой странице |
| **Понятный язык** | «Добавлена колонка "Статус" на странице Список ресторанов» |
| **Единый формат** | Одинаковая логика и структура для всех прототипов |

---

## Предлагаемое решение

### 1. Формат данных — TypeScript вместо Markdown

Вместо `changelogs/*.md` — **структурированные TypeScript-файлы** внутри каждого прототипа.

**Почему TypeScript, а не Markdown:**
- Type-safe: ошибки ловятся при компиляции
- Кликабельные ссылки реализуются через Angular Router
- Группировка по страницам — встроенная структура, а не парсинг текста
- Рендеринг в UI — напрямую, без парсера markdown
- Copilot при изменении прототипа сразу обновляет `changelog.data.ts` в том же PR

**Файл**: `src/app/prototypes/<slug>/changelog.data.ts`

```typescript
import { ChangelogRelease } from '@/shared/changelog.types';

export const CHANGELOG: ChangelogRelease[] = [
  {
    version: '1.8',
    date: '2026-02-17',
    status: 'released',
    changes: [
      {
        page: 'Список ресторанов',
        pageRoute: '/prototype/pudu-admin',
        items: [
          'Добавлена карточка статуса подключения к NE (connected / error / not configured)',
          'Модальное окно ввода client_id и api_secret с проверкой',
          'Блокировка перехода к ресторану без активного подключения',
        ],
      },
      {
        page: 'Маркетинг',
        pageRoute: '/prototype/pudu-admin/restaurant/1/marketing',
        items: [
          'Замена одиночного выбора робота на множественный с чипами',
          'Предупреждение при удалении последнего робота',
        ],
      },
      {
        page: 'Список роботов',
        pageRoute: '/prototype/pudu-admin/restaurant/1',
        items: [
          'Inline-редактирование имени робота — клик по ячейке → поле ввода',
        ],
      },
      {
        // Без привязки к конкретной странице
        items: [
          'Mock-переключатель NE API с предупреждением при недоступности',
          'Toast "Настройка сохранена" при сохранении',
        ],
      },
    ],
  },
  {
    version: '1.7',
    date: '2026-02-17',
    status: 'released',
    changes: [
      {
        page: 'Маппинг столов',
        pageRoute: '/prototype/pudu-admin/restaurant/1/mapping',
        items: [
          'Исправлена привязка столов в режиме «Точки → Столы»',
          'Фильтр по залу теперь влияет на выпадающие списки',
          'Предупреждение о незамапленных столах в обоих режимах',
        ],
      },
      {
        items: [
          'Breadcrumbs стали кликабельными на всех экранах',
          'Всплывающие подсказки на значения статуса настройки',
        ],
      },
    ],
  },
];
```

### 2. Типы данных

**Файл**: `src/app/shared/changelog.types.ts`

```typescript
export interface ChangelogRelease {
  /** Версия, например "1.8", "2.0" */
  version: string;
  /** Дата релиза, формат YYYY-MM-DD */
  date: string;
  /** Статус: released = в master, unreleased = только в dev */
  status: 'released' | 'unreleased';
  /** Группы изменений (по страницам) */
  changes: ChangeGroup[];
}

export interface ChangeGroup {
  /** Название страницы для группировки (опционально) */
  page?: string;
  /** Маршрут страницы — делает название кликабельным (опционально) */
  pageRoute?: string;
  /** Список изменений в свободной форме */
  items: string[];
}
```

### 3. Где показывать версию и дату — Top Bar

Текущий top-bar:
```
[Роботы PUDU — Admin Panel]                      [Reset] [Прототип]
```

Предлагаемый top-bar:
```
[Роботы PUDU — Admin Panel]          [Reset] [v1.8 · 17.02.2026  📋] [Прототип]
```

Элементы:
- **`v1.8`** — текущая версия (берётся из первого элемента `CHANGELOG`)
- **`17.02.2026`** — дата последнего обновления
- **`📋`** (иконка `scroll-text` или `file-text`) — кнопка открытия changelog-модалки
- Вся конструкция `v1.8 · 17.02.2026 📋` — это единый компактный блок

**Реализация**: новый компонент `ChangelogButtonComponent` в `src/app/components/layout/`.

### 4. Модальное окно Changelog

При клике на кнопку/версию открывается `UiModal` (size: `lg`) с полной историей изменений.

#### Макет модального окна:

```
┌──────────────────────────────────────────────────┐
│  История изменений — Роботы PUDU            [✕]  │
├──────────────────────────────────────────────────┤
│                                                  │
│  ▼ v1.8 — 17 февраля 2026            ✅ Master  │
│  ┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄┄   │
│                                                  │
│    📄 Список ресторанов ←(кликабельная ссылка)   │
│    • Добавлена карточка статуса подключения NE    │
│    • Модальное окно ввода client_id + api_secret  │
│    • Блокировка перехода без подключения          │
│                                                  │
│    📄 Маркетинг ←(кликабельная ссылка)           │
│    • Замена одиночного выбора на множественный    │
│    • Предупреждение при удалении последнего       │
│                                                  │
│    📄 Список роботов ←(кликабельная ссылка)      │
│    • Inline-редактирование имени робота           │
│                                                  │
│    Общие изменения                               │
│    • Mock-переключатель NE API                   │
│    • Toast "Настройка сохранена"                  │
│                                                  │
│  ▶ v1.7 — 17 февраля 2026            ✅ Master  │ ← свёрнуто
│  ▶ v1.6 — 13 февраля 2026            ✅ Master  │ ← свёрнуто
│  ▶ v1.0 — 16 февраля 2026            ✅ Master  │ ← свёрнуто
│                                                  │
├──────────────────────────────────────────────────┤
│                                         [Закрыть]│
└──────────────────────────────────────────────────┘
```

**Ключевые UX-решения:**
- Последняя версия — **раскрыта по умолчанию**, остальные свёрнуты
- Клик по заголовку версии раскрывает/сворачивает секцию (аккордеон)
- Название страницы — **синяя кликабельная ссылка** → переход + закрытие модалки
- Без привязки к странице → под заголовком «Общие изменения» (серый текст)
- Бейдж статуса: `✅ Master` (зелёный) или `🔶 В разработке` (оранжевый)
- Дата отформатирована по-русски: «17 февраля 2026»

### 5. Версионирование

**Формат**: `MAJOR.MINOR` (например, `1.0`, `1.5`, `2.0`)

| Что изменилось | Как версионировать |
|---|---|
| Создание прототипа | `1.0` |
| Небольшие правки (баги, тексты, стили) | `+0.1` (1.0 → 1.1) |
| Новый экран или существенная функциональность | `+0.1` (1.1 → 1.2) |
| Крупная переработка / перестройка прототипа | `+1.0` (1.2 → 2.0) |

Версия не имеет жёсткой семантики — это просто порядковый номер для ориентира. Главное — она растёт и позволяет заказчику понять «что-то обновилось».

### 6. Правила ведения Changelog (для Copilot)

При **каждом изменении** прототипа, Copilot:

1. Открывает `src/app/prototypes/<slug>/changelog.data.ts`
2. Если есть секция с `status: 'unreleased'` — добавляет изменения туда
3. Если нет — создаёт новую секцию `unreleased` с новой версией
4. Записывает изменения:
   - Группирует по страницам (если изменения на конкретной странице)
   - Указывает `pageRoute` для кликабельности
   - Пишет понятным языком: **что** изменилось и **где**
5. При мерже `dev → master` — меняет `status: 'unreleased'` → `'released'`

**Примеры хороших записей:**
```
✅ «Добавлена колонка "Статус NE" в таблицу роботов»
✅ «Кнопка "Сохранить" теперь показывает подтверждение»
✅ «Исправлен фильтр по залу — теперь влияет на выпадающие списки»
✅ «Новый экран: настройки подключения к NE»

❌ «Обновлён компонент robot-list-screen.component.ts»
❌ «Рефакторинг» (слишком общо)
❌ «F12: Warning-блок» (непонятная кодировка)
```

### 7. Структура файлов после реализации

```
src/app/
├── shared/
│   └── changelog.types.ts                    ← НОВЫЙ: типы
├── components/
│   └── layout/
│       ├── changelog-button.component.ts      ← НОВЫЙ: кнопка версии в top-bar
│       ├── changelog-modal.component.ts       ← НОВЫЙ: модалка с историей
│       └── top-bar.component.ts               ← ИЗМЕНЁН: добавлен changelog-button
├── prototypes/
│   ├── demo/
│   │   └── changelog.data.ts                  ← НОВЫЙ
│   ├── front-plugins/
│   │   └── changelog.data.ts                  ← НОВЫЙ
│   ├── web-screens/
│   │   └── changelog.data.ts                  ← НОВЫЙ
│   ├── pudu-admin/
│   │   └── changelog.data.ts                  ← НОВЫЙ
│   └── front-pudu-plugin/
│       └── changelog.data.ts                  ← НОВЫЙ
changelogs/                                     ← УДАЛИТЬ (заменены на .ts)
```

### 8. Что происходит с существующими changelogs/*.md

**Предлагаю**: миграция данных из markdown в TypeScript-формат, после чего `changelogs/` удаляется. Вся история сохраняется в `changelog.data.ts` каждого прототипа.

Markdown-файлы больше не нужны, потому что:
- История теперь видна **в самом прототипе** (для заказчика)
- Разработчик видит её в коде (TS-файл в папке прототипа)
- Нет дублирования данных

---

## Обновление copilot-instructions.md

В инструкции нужно добавить:

1. Заменить секцию «Changelogs (Patch Notes)» на новую с описанием TypeScript-формата
2. Добавить в «Структура прототипа» файл `changelog.data.ts`
3. Добавить в «Правила качества» пункт про обновление changelog
4. Добавить в «Регистрация нового прототипа» шаг создания `changelog.data.ts`

---

## Варианты размещения кнопки (на выбор)

### Вариант A: В Top Bar (рекомендуется ✅)

```
[Роботы PUDU — описание]          [Reset] [v1.8 · 17.02.2026 📋] [Прототип]
```

**Плюсы:** всегда видно, не мешает, унифицировано для всех прототипов
**Минусы:** top-bar становится чуть плотнее

### Вариант B: В Footer (внизу страницы)

Небольшая строка внизу каждого прототипа:
```
─────────────────────────────────────
v1.8 · Обновлено 17.02.2026 · Показать changelog
```

**Плюсы:** совсем не мешает
**Минусы:** не сразу заметно, нужно скроллить

### Вариант C: Плавающая кнопка (FAB)

Маленькая круглая кнопка в углу экрана (📋), при наведении показывает `v1.8 · 17.02.2026`.

**Плюсы:** всегда доступна, не занимает место в layout
**Минусы:** может перекрывать контент, непривычный паттерн для Web

### Рекомендация

**Вариант A** — в Top Bar. Причины:
- Top-bar уже содержит мета-информацию о прототипе (название, Reset, бейдж)
- Логичное расположение: версия — это мета-информация
- Не требует изменений в layout экранов прототипов
- Работает из коробки для всех прототипов (один компонент в top-bar)

---

## Резюме

| Аспект | Решение |
|---|---|
| Формат данных | TypeScript (`changelog.data.ts` в папке прототипа) |
| Где версия | Top-bar, компактный блок: `v1.8 · 17.02.2026` |
| Где changelog | Модальное окно (UiModal), открывается кнопкой из top-bar |
| Группировка | По страницам, с кликабельными ссылками на страницы |
| Версионирование | `MAJOR.MINOR`, инкремент при каждом релизе |
| Язык записей | Понятный русский, без кодовых ссылок |
| Кто ведёт | Copilot — автоматически при каждом изменении прототипа |
| Миграция | Данные из `changelogs/*.md` → `changelog.data.ts`, md-файлы удаляются |
